// let score = Array(10).fill(0);

// let scores = [10,1,2,3,4,5,,,,].fill(0);
// # fill overwrited the values here

// let scores = [10,1,2,3,4,5,,,,].fill(0,0,3);
let scores = [10,11,1,2,3,4,5,,,,].fill(0,2,3);
// # explanation for (0,0,3) => insert 0 for the elements from 0 to 3, but not include the last one  

console.log(scores)